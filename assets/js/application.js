(function(root){
'use strict';
const U=root.Monganga,D=root.MongangaData,A=root.MongangaApi,C=root.MongangaCatalog;
const {$,esc,field,area,select,check,submit,heading,render,link,bindForm}=U;
U.pages.application=async()=>{
 const docs=[['portrait','Photo professionnelle'],['identity','Pièce d’identité'],['medicalDiploma','Diplôme de médecine'],['orderCertificate','Attestation d’inscription à l’Ordre'],['cv','Curriculum vitæ'],['specializationDiploma','Diplôme de spécialisation']];
 render(`${heading('Rejoignez les médecins Monganga.','Préparez votre dossier professionnel. Votre candidature sera examinée avant l’ouverture d’un compte médecin.')}<div class="layout-two"><section class="panel"><form id="application-form" class="stack"><h2>1. Vos coordonnées</h2><div class="form-grid">${field('firstName','Prénom','text','','required autocomplete="given-name" maxlength="80"')}${field('lastName','Nom','text','','required autocomplete="family-name" maxlength="80"')}${field('email','Adresse email','email','','required autocomplete="email"')}${field('phone','Téléphone','tel','','required autocomplete="tel" pattern="[+0-9 ()\\-]{9,20}"')}${field('commune','Commune d’exercice','text','','required maxlength="80"')}${field('orderNumber','Numéro d’inscription à l’Ordre','text','','required maxlength="80"')}</div><h2>2. Votre pratique</h2>${select('specialty','Spécialité',[...new Set(C.doctors.map(d=>d.specialty))],'Médecine générale','required')}${area('bio','Présentation et expérience professionnelle','','required minlength="30" maxlength="4000"')}<h2>3. Vos justificatifs</h2><p class="meta">PDF, JPG, PNG ou WebP, 10 Mo maximum par document. La photo doit être une image. Les fichiers restent sélectionnés si une erreur survient.</p>${docs.map(([key,label])=>`<div id="group-${key}">${field(key,label,'file','',`required accept="${key==='portrait'?'image/jpeg,image/png,image/webp':'application/pdf,image/jpeg,image/png,image/webp'}"`)}<p class="meta mt-2" id="file-${key}"></p></div>`).join('')}${check('attestation','Je confirme l’exactitude des informations et l’authenticité des pièces transmises.',false,'required')}${check('consent','J’accepte le traitement de ce dossier pour l’examen de ma candidature.',false,'required')}${submit('Déposer ma candidature')}</form></section><aside class="panel stack"><h2>Les prochaines étapes</h2><p><strong>01 · Dépôt du dossier</strong><br><span class="muted">Rassemblez vos coordonnées et justificatifs.</span></p><p><strong>02 · Examen de la candidature</strong><br><span class="muted">L’équipe vérifie vos qualifications et peut demander un complément.</span></p><p><strong>03 · Accès professionnel</strong><br><span class="muted">Votre compte est ouvert après validation.</span></p>${!A.isConfigured()?'<p class="notice">En démonstration, utilisez des documents fictifs. Seuls leurs noms sont conservés dans cet onglet ; aucun fichier n’est transmis.</p>':''}${link('Une question ?','public/contact.html')}</aside></div>`);
 const specialty=()=>{const required=$('#specialty').value!=='Médecine générale';$('#group-specializationDiploma').hidden=!required;$('#specializationDiploma').required=required;$('#specializationDiploma').disabled=!required;};$('#specialty').onchange=specialty;specialty();
 docs.forEach(([key])=>{$('#'+key).onchange=()=>{const input=$('#'+key),file=input.files[0],feedback=$('#file-'+key);input.setCustomValidity('');if(!file){feedback.textContent='';return;}try{D.validateFile(file,key==='portrait');feedback.textContent=`${file.name} · ${(file.size/1024/1024).toFixed(2)} Mo`;}catch(e){input.setCustomValidity(e.message);feedback.textContent=e.message;input.reportValidity();}};});
 bindForm('application-form',async(_,form)=>{const result=await D.call('application',new FormData(form),{headers:{'Idempotency-Key':form.dataset.requestId||(form.dataset.requestId=crypto.randomUUID())}});render(`${heading(result.demo?'Dossier de démonstration enregistré.':'Candidature déposée.')}<section class="panel stack"><p class="notice success">${result.demo?'Retrouvez ce dossier dans l’espace Administration → Validations. Aucun fichier ni email n’a été envoyé.':'Votre dossier a été transmis pour examen. Conservez votre référence.'}</p><p>Référence : <strong>${esc(result.id)}</strong></p><div class="actions">${link('Retour à l’accueil','index.html','primary')}${result.demo?link('Changer de compte','auth/login.html'):''}</div></section>`);});
};
})(window);
